export const REFERENCE_SITE_PATH = ["media", "reference", "pangoscript-reference.html"] as const;

export const DIAGNOSTIC_DOCS_PATH = ["docs", "references", "diagnostics", "README.md"] as const;

export const SIDEBAR_MEDIA_ROOT_PATH = ["media", "sidebar"] as const;
export const DIST_PATH = ["dist"] as const;

export const SIDEBAR_COMMANDS_CSS_PATH = ["media", "sidebar", "commands.css"] as const;
export const SIDEBAR_COMMANDS_SCRIPT_PATH = ["dist", "sidebar-commands-webview.js"] as const;

export const SIDEBAR_OBJECTS_CSS_PATH = ["media", "sidebar", "objects.css"] as const;
export const SIDEBAR_OBJECTS_SCRIPT_PATH = ["dist", "sidebar-objects-webview.js"] as const;
